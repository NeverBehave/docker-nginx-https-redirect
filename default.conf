server {
    listen         80;
    server_tokens  off;

    # no robot rules here.
    location ~ \.php$ {
      try_files $url /index.html;   
    }
    
    return 301 https://$host$request_uri;
}
